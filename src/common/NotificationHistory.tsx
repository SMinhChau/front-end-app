import React from 'react';
import {Notification} from '../types/Types';

interface Props {
  navigation: any;
  isRTL: boolean;
  strings: any;
  notifications: Notification[];
}

const NotificationHistory: React.FC<Props> = ({}) => {
  return <></>;
};
export default NotificationHistory;
